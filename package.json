{
  "name": "svelte-app",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf public/build && rollup -c",
    "dev": "rollup -c -w",
    "start": "sirv public",
    "gh-pages": "yarn build && rimraf docs && cp -R public docs"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "11.0.2",
    "@rollup/plugin-node-resolve": "^7.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^1.20.0",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^5.1.2",
    "svelte": "^3.0.0"
  },
  "dependencies": {
    "acorn": "^7.2.0",
    "decimal.js": "^10.2.0",
    "sirv-cli": "^0.4.4"
  }
}